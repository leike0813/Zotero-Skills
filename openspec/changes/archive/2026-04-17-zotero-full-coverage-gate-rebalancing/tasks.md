## 1. Full Coverage Gate Expansion

- [x] 1.1 Expand Zotero `full` allowlists for stable `core` suites
- [x] 1.2 Expand Zotero `full` title allowlists for stable `ui` suites
- [x] 1.3 Expand Zotero `full` title allowlists for stable `workflow` suites

## 2. Governance Recording

- [x] 2.1 Record `full` as the stable CI host-coverage gate in governance docs
- [x] 2.2 Record the `full` coverage buckets and retained suite inventory
- [x] 2.3 Add OpenSpec artifacts and delta specs for the new full-gate contract

## 3. Validation

- [x] 3.1 Run targeted Node regressions for allowlist changes
- [x] 3.2 Run Zotero `core:full`, `ui:full`, and `workflow:full`
- [x] 3.3 Run `openspec validate zotero-full-coverage-gate-rebalancing --strict` and `npx tsc --noEmit`
