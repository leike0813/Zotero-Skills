- [x] Dashboard默认窗口大小扩大，显示区域随窗口resize
- [x] Tag Manager 的暂存窗口只有在主窗口关闭后才能打开。若现有框架无法满足，就整体重构为网页方式
- [x] Tag Manager 的暂存窗口仍然是“变动后需要save”的逻辑，但这个窗口其实不需要，随时可以关闭，也不需要save（因为是实时变动的）
- [x] Tag Regulator 的 suggest tags 窗口在倒计时结束后不会自动关闭
- [ ] Dashboard 增加 Tag Manager 快捷方式（可选，要做的话也得做成注册式的）
- [x] passthrough provider 且自身无设置项的 workflow 不显示设置菜单项
- [x] 添加 `literature-explainer` workflow
- [x] 对话窗口效果进一步优化（现在对话窗口和提示卡片的上下关系反了，而且大小也反了）
- [x] **修复 opencode 无法传模型的问题，本质上是目前的插件前端没有分离 provider**
- [x] **匹配最新 Skill-Runner 文件协议**
- [ ] 添加 Tag 受控词表订阅和发布功能
- [p] **日志功能进一步完善，携带更多排障信息**，转为 web 实现并入 Dashboard
- [ ] 移除“采样选区上下文”及“校验选区上下文”按钮，或者将这两个按钮移入插件二级菜单，通过插件构建时的硬编码开关决定是否显示
- [ ] 添加 Workflow Execute 快捷按钮
- [x] **后端自动下载、部署和配置脚本**
- [x] 运行时信息逻辑（如果没有运行时信息，必须先执行部署其他按钮才能用；如果有运行时信息，运行脚本（skill-runnerctl和卸载）时默认脚本存在，若找不到脚本直接抛错并展示；新部署会覆盖旧运行时信息；卸载会清除运行时信息；运行时信息与后端设置绑定（不允许更改被绑定的backend设置，或者甚至改成这个后端设置单列，不进后端配置））。运行时信息需要持久化
- [x] 启动/部署按钮合并为“一键部署/启动”按钮，按下时如果有运行时信息，先 preflight, 若通过则直接 up，若不通过则进入部署；若没有运行时信息直接进入部署
- [x] 拉起需要先 preflight 通过，up 后立刻 require (现有逻辑)
- [x] 手动结束先 release 再 down 保底
- [x] 运行状态切换监控要有
- [x] 自动拉起保持现在的轻量化逻辑，与 lease 无关，稳定后切换为默认开启（前提是必须有运行时信息）；有运行时信息但任意一次 preflight 失败后，关闭自动拉起；有运行时信息但任意一次 preflight 成功后，开启自动拉起；
- [x] 上述需要固化为状态机，确保以后不飘
- [x] 拉起后端加 toast
- [x] 后端意外结束加 toast
- [ ] 启动和卸载不应该阻塞
- [x] 所有动作同一时间仅允许执行一个，按钮暂时 disable
- [ ] 卸载加弹窗，弹窗内选择是否清理数据和 agent 设置
- [ ] 自动部署日志交还日志系统，添加进度条和文字提示
- [ ] 梳理自动部署功能的各种返回文案，多语言化
- [ ] 卸载的一点 bug 排查
- [ ] **重构 workflow 设置逻辑（触发时弹窗，一个批次用同样的设置）**
- [ ] **重构窗口逻辑（围绕Dashboard），简化右键菜单，插件窗口随 zotero主窗口关闭而关闭**。skill runner的任务页面选项卡化
- [x] 修复修改 skill runner 后端后，旧的后端仍然会残留在 Dashboard 中的问题 
- [ ] MIT License
