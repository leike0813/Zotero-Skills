# workflow-debug-probe-diagnostic-workflow

## Summary

Add a debug-only builtin diagnostic workflow that can run even when other workflows are disabled by preflight, so runtime failures can be inspected from a dedicated probe entrypoint.
